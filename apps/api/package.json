{
  "name": "@hireboost/api",
  "version": "0.1.0",
  "private": true,
  "description": "HireBoost AI — Backend (Express + TypeScript + MongoDB).",
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "dev": "echo \"[api] dev server is added in Phase 3 (Express + tsx). Skipping for now.\" && exit 0",
    "build": "tsc -p tsconfig.json",
    "start": "echo \"[api] runtime is added in Phase 3. Skipping for now.\" && exit 0",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hireboost/shared": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "typescript": "^5.6.3"
  }
}
