{
  "installCommand": "npm install",
  "buildCommand": "npm run build:shared && npm run build --workspace @hireboost/web",
  "outputDirectory": "apps/web/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
